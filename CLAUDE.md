@AGENTS.md

The project's agent instructions live in `AGENTS.md`, imported above so that
Claude Code and every other agent tool read the same file. If for any reason the
import did not resolve, read `AGENTS.md` now before changing any code.

Do not add content here. Put rules and contracts in `AGENTS.md`, per-subsystem
reference in `ARCHITECTURE.md`, and completed-work narrative in
`ENGINEERING-LOG.md`.
